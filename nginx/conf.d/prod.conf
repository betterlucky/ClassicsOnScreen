server {
    listen 80;
    server_name classicsonscreen.theworkpc.com www.classicsonscreen.theworkpc.com;
    
}

server {
    listen 443 ssl;
    server_name classicsonscreen.theworkpc.com www.classicsonscreen.theworkpc.com;

    ssl_certificate /etc/letsencrypt/live/classicsonscreen.theworkpc.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/classicsonscreen.theworkpc.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
        client_max_body_size 10M;
    }

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000";
    }
} 